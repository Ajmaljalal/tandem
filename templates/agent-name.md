# [Agent Name] scratchpad

Owned by: [Agent Name]

Other agents may read this file but should not edit it.

If `<name>.md` already exists, append a new dated section below by default. Only if the existing
file is a read-only role prompt or stale artifact, use `<name>-scratchpad.md` instead and record
that alias in `handoff.md`. Never overwrite an existing scratchpad or a partner's file.

## [date/time]

- Role/lane: [what this agent owns]
- Current lock/branch: [lock/branch]
- Notes:
  - [private working note]
